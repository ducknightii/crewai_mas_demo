[pytest]
markers =
    e2e: Real execution tests requiring Docker sandbox + LLM API (run with -m e2e)
    integration: Integration tests requiring LLM API (run with -m integration)
asyncio_mode = strict
